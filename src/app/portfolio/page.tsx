import PortfolioClientPage from './client';

// This is a server component
export default function PortfolioPage() {
  // In the future, you could do server-side data fetching here.
  return <PortfolioClientPage />;
}
